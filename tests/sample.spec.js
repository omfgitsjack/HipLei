import expect from 'expect.js';

describe('actions', () => {
  it('should create an aciton to add a todo', () => {
    expect(true).to.eql(true);
  })

  it('should create an actionn to add a todo ', () => {
    expect(true).to.eql(true);  
  })
})  